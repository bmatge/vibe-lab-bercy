# Secret pour signer les tokens JWT (obligatoire)
# Generer avec : python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# Utilisateur par defaut cree au premier demarrage (si la table users est vide)
DEFAULT_USER_EMAIL=admin@vibelab.bercy.matge.com
DEFAULT_USER_PASSWORD=change-me

# Repertoire de donnees dans le conteneur (ne pas modifier sauf raison)
DATA_DIR=/app/data
